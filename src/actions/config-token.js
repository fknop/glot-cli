const Files = require('../utils/files');

module.exports.action = function (args, cb) {
    
    // Files.writeConfig({ token: args.token });
    this.log('This function is not yet supported');
    // this.log(`Token added.`);
    cb(); 
};